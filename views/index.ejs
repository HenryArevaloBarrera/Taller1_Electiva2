<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("templates/header") %>
    <style>
        body {
            background-color: #2c2c2c; /* gris oscuro */
            color: #f5f5f5; /* texto claro */
        }

        .image-container {
            display: flex;
            flex-direction: column; /* 🔑 columna, una debajo de la otra */
            align-items: center;
            margin: 20px 0;
            gap: 20px; /* espacio entre tarjetas */
        }

        .card {
            width: 100%;
            max-width: 1000px; /* ancho máximo centrado */
            background-color: #ffffff; /* tarjetas blancas */
            color: #000000; /* texto negro */
            display: flex;
            flex-direction: column;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.3);
            min-height: 500px; /* más altas */
        }

        .card img {
            height: 420px;
            object-fit: contain;
            width: 100%;
            background-color: #f8f9fa;
            border-bottom: 1px solid #ddd;
        }

        .card-body {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        @media (max-width: 768px) {
            .card {
                min-height: 350px;
            }

            .card img {
                height: 220px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <header>
            <!-- Encabezado -->
        </header>

        <!-- Contenedor de tarjetas -->
        <div class="image-container">

            <div class="card">
                <img src="/img/clases.png" alt="Diagrama de Clases">
                <div class="card-body text-center">
                    <h5 class="card-title">Diagrama de Clases</h5>
                </div>
            </div>

            <div class="card">
                <img src="/img/despliege.png" alt="Despliegue del Sistema">
                <div class="card-body text-center">
                    <h5 class="card-title">Despliegue del Sistema</h5>
                </div>
            </div>

        </div>

        <footer>
            <%- include("templates/footer") %>
        </footer>
    </div>
</body>

</html>
